// Tests that import worker source (e.g. tier4 f_real_world_scenarios) bring
// worker files into the frontend program; they need Cloudflare globals (Env,
// R2 types). Package is hoisted at the repo root node_modules.
/// <reference types="@cloudflare/workers-types" />
